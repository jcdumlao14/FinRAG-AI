services:

  finrag:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - chroma_data:/app/chroma_db
      - ./monitoring:/app/monitoring

    restart: unless-stopped

volumes:
  chroma_data:
